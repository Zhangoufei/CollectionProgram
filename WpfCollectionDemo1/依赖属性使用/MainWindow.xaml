<Window x:Class="依赖属性使用.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:依赖属性使用"
        xmlns:local1="clr-namespace:依赖属性使用.Model"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <UniformGrid  >
            <Button Content="使用依赖属性" Click="Button_Click" />
            <TextBlock x:Name="textBolock" Text="{Binding Name}" />
            <local1:MyButton  Width="250"  Content="使用自定义依赖属性,字体大小不改变" />
            <local1:MyLable  Content="Mylable"   />
            <Label Content="Label内容" Width="250" />
            <Button Content="使用依赖属性" />
            <Button Content="listBox按钮" Click="Button_Click_1" />
        </UniformGrid>
    </Grid>
</Window>
